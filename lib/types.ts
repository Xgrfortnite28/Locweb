export type Service = {
  title: string;
  description: string;
  icon: string;
};

export type Review = {
  name: string;
  initials: string;
  text: string;
};

export type FAQ = {
  question: string;
  answer: string;
};
